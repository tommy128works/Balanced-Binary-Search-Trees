import Node from "./Node.js";

const Tree = (array) => {
  let root = null;

  const buildTree = (array) => {

    array.sort(function(a, b) {return a - b});

    console.log(array);


    // return root
  }

  buildTree(array);

  // return { append };
};

export default Tree;
